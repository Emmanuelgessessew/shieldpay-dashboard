PORT=8790
DATABASE_PATH=./backend/data/shieldpay.db

# Seeded admin credentials
ADMIN_EMAIL=admin@shieldpay.local
ADMIN_PASSWORD=Admin1234!

# IMPORTANT: In production, JWT_SECRET must be long and random.
# This demo intentionally uses a weak fallback if JWT_SECRET is missing (see ARKO-LAB-07).
JWT_SECRET=change-me-to-a-strong-secret

